

import 'package:flutter/material.dart';

import 'child/home.dart';

class XmlTxt extends StatelessWidget {
  const XmlTxt({super.key});

  @override
  Widget build(BuildContext context) {
    return XmlTxtUploadView();
  }
}
